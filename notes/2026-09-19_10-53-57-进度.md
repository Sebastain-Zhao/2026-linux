# 2026-09-19 10:53:57 CST 学习进度

## 当前阶段

- 第一阶段 / Project 01：认识我的 Linux 网络
- 当前微实验：01 —— 识别 Ubuntu 主机的网络接口与 IPv4 地址
- 实验状态：待学员执行与验收
- 环境状态：审计完成，安装脚本待执行

## 已完成

### 项目与 GitHub

- 已建立课程 `README.md`、`TODO.md` 和 `notes/` 笔记目录。
- 当前项目已连接 GitHub 仓库 `Sebastain-Zhao/2026-linux`。
- Git 全局作者已配置为 `seb <zhaozhanglu0813@outlook.com>`。
- GitHub SSH 密钥已配置并验证成功。
- Codex 全局 Git 同步规则已写入 `~/.codex/AGENTS.md`。
- 截至本记录创建前，远端 `main` 最新提交为 `5c61a85 Add Ubuntu lab environment setup`。

### Ubuntu 实验环境

- 已确认 Ubuntu 24.04.4 LTS、x86_64，当前内核 headers 已安装。
- 已确认有线网卡、Wi-Fi、代理 TUN 接口和默认路由可见。
- 已确认 BlueZ、蓝牙服务和蓝牙控制器可用。
- 已确认 Python 3.12、GCC、GDB、Make、Git、OpenBSD netcat 等基础工具可用。
- 已创建 `scripts/setup_ubuntu_lab.sh`，用于安装通用网络、抓包、编译、内核、BLE 和串口工具。
- 已创建 `scripts/check_lab_environment.sh`，用于统一验收环境。
- 安装脚本已补充 `wireshark` 和 `dialout` 用户组配置，分别用于普通用户抓包和串口访问。

### 已记录的诊断案例

- 浏览器可访问 Google、但 `ping` 无响应的分层诊断。
- Codex CLI、代理链路和 Ubuntu AppArmor/bubblewrap sandbox 的诊断与修复。

## 尚未完成

- 尚未以 sudo 执行基础环境安装脚本。
- 尚未注销并重新登录，因此 `wireshark`、`dialout` 新组权限尚未验收。
- 环境验收脚本尚未得到 `ENVIRONMENT_OK`。
- 实验 01-1 的 `ip addr` 输出和五道思考题尚未提交。
- Project 01 后续的 `ip link`、`ip route`、`ping` 和错误网段实验尚未开始。
- Project 02 至 Project 09 尚未开始。

## 当前缺失或待补充的通用工具

最近一次审计发现以下工具待安装：

- `traceroute`、`socat`、`iperf3`
- Wireshark、TShark
- CMake、pkg-config、Ninja
- Valgrind
- Bison、Flex、OpenSSL/ELF 开发库、pahole
- Bluetooth 开发库
- picocom、minicom

这些工具均已列入 `scripts/setup_ubuntu_lab.sh`。

## 暂不提前安装

以下内容依赖具体开发板，当前不猜测架构或厂商：

- ARM、AArch64 或 RISC-V 交叉编译器
- 厂商 SDK、BSP 和烧录工具
- GPIO 库与设备树实验配置
- USB 转串口芯片的专用规则

确定开发板型号、CPU 架构和烧录方式后再配置。

## 下一步

在项目根目录执行：

```bash
sudo ./scripts/setup_ubuntu_lab.sh
```

安装完成后注销并重新登录，再执行：

```bash
./scripts/check_lab_environment.sh
```

验收环境后，回到 Project 01 实验 01-1，只执行 `ip addr` 并回答观察题。

